// doubles as front page
<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1" />
    <title><%= title %></title>
  </head>
  <body>
    <h1><%= title %></h1>
    <% if (items && items.length > 0) { %>
    <ul>
      <% items.forEach((item) => { %>
      <li><%= item.game_name %></li>
      <% }) %>
    </ul>
    <% } %>
  </body>
</html>
